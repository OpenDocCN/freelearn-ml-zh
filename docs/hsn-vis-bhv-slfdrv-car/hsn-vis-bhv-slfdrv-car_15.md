# 第十二章：评估

# 第一章

1.  是的，尽管在某些情况下，你可能需要一个定制的构建。

1.  通常使用`bilateralFilter()`。

1.  HOG 检测器。

1.  使用`VideoCapture()`。

1.  较大的光圈增加了传感器可用的光线量，但减少了景深。

1.  当没有足够的光线来满足所需的快门速度和光圈设置时，你需要更高的 ISO。

1.  是的，亚像素精度以显著的方式提高了校准。

# 第二章

1.  对于**UART**：**单端**：两根线（数据和地）。由于它是异步的，设备保持自己的时间，并在事先就波特率达成一致，因此不需要时钟线。**差分**：两根线（数据高和数据低）。测量的是差分电压，而不是相对于地的电压。

    **I2C**：两根线，**串行时钟**（**SCL**）和**串行数据**（**SDA**），使用具有主从设备的总线架构。

    **SPI**：3 + 1*n*根线，其中*n*是从设备的数量。三条主要线：**信号时钟**（**SCLK**）、**主设备输出从设备输入**（**MOSI**）和**主设备输入从设备输出**（**MISO**）；以及每个从设备的一个**从设备选择**（**SS**）线。

    **CAN**：两根线，CAN-HI 和 CAN-LO，使用 CAN-HI 和 CAN-LO 作为差分对的总线架构。

1.  通过使用噪声对两个信号产生相似影响的差分对，可以减少噪声。线相互缠绕以消除任何感应电流。

1.  串行传输按顺序逐个通过单根线发送所有位，串行。并行传输同时将每个位发送到其自己的线。因此，对于 8 位字，并行传输将快 8 倍。

1.  I2C、SPI、CAN 和以太网。

1.  I2C 和 SPI。

1.  UART。

# 第三章

1.  HLS、HSV、LAB 和 YcbCr。

1.  为了获得车道线的鸟瞰图，以便它们在图像中也保持平行。

1.  使用直方图。

1.  滑动窗口。

1.  使用`polyfit()`，然后使用系数来画线。

1.  `Scharr()`效果很好。

1.  **指数加权移动平均**简单且有效。

# 第四章

1.  它是神经网络中的一个神经元。

1.  Adam。

1.  这是一个将核应用于某些像素的操作，从而得到一个新的像素作为结果。

1.  它是一个至少包含一个卷积层的神经网络。

1.  它是一个层，将某一层的所有神经元连接到前一层的所有神经元。

1.  它将卷积层的 2D 输出线性化，以便可以使用密集层。

1.  TensorFlow。

1.  LeNet。

# 第五章

1.  你必须做你必须做的事情...但理想情况下，你只想使用一次，以避免在模型选择上的偏差。

1.  它是从初始数据集中生成更多数据的过程，以增加其大小并提高网络的泛化能力。

1.  不完全是这样：Keras 用数据增强得到的新图像替换了原始图像。

1.  通常，密集层往往是参数最多的层；特别是，在最后一个卷积层之后的第一个通常是最大的。

1.  当训练损失的线随着 epoch 数的增加而下降时，验证损失上升。

1.  不一定：你可以使用一种策略，首先过度拟合网络（以正确学习训练数据集），然后提高泛化能力并消除过度拟合。

# 第六章

1.  为了增加非线性激活的数量，并让网络学习更复杂的函数。

1.  不一定。事实上，一个精心设计的深度网络可以更快且更精确。

1.  当训练准确率提高但验证准确率下降时。

1.  提前停止。

1.  使用批量归一化。

1.  使用数据增强。

1.  因为它学会了不仅仅依赖少数几个通道。

1.  训练可能需要更慢的速度。

1.  训练可能需要更慢的速度。

# 第七章

1.  SSD 是一种能够在图像中找到多个对象的神经网络，其输出包括检测到的对象位置。它可以实时工作。

1.  Inception 是由谷歌创建的一个有影响力和精确的神经网络。

1.  一个被冻结的层无法进行训练。

1.  不，它不能。它只能检测交通灯，但不能检测其颜色。

1.  迁移学习是将一个在某个任务上训练好的神经网络适应解决一个新、相关任务的过程。

1.  添加 dropout，增加数据集的大小，增加数据增强的多样性，并添加批量归一化。

1.  考虑到 ImageNet 中图像的多样性，很难选择卷积层的核大小，所以他们并行使用了多个大小的核。

# 第八章

1.  DAVE-2，但也可以称为 DriveNet。

1.  在分类任务中，图像根据一些预定义的类别进行分类，而在回归任务中，我们生成一个连续的预测；在我们的例子中，例如，一个介于-1 和 1 之间的转向角度。

1.  你可以使用`yield`关键字。

1.  这是一个可视化工具，可以帮助我们了解神经网络关注的地方。

1.  我们需要三个视频流来帮助神经网络理解如何纠正错误的位置，因为侧摄像头实际上是从远离汽车中心的位置进行校正。

1.  由于性能原因，并且确保所有代码只运行在客户端。

# 第九章

1.  密集块，其中每一层都连接到前一层的所有输出，包括输入。

1.  ResNet。

1.  这是对 DenseNet 进行语义分割的改进。

1.  因为它可以像 U 形一样可视化，左侧下采样，右侧上采样。

1.  不，你只需堆叠一系列卷积即可。然而，由于高分辨率，实现良好的结果将具有挑战性，并且很可能会使用大量内存并且相当慢。

1.  你可以使用中值模糊来去除可能存在于训练不良网络的分割掩码中的*盐和胡椒*噪声。

1.  它们用于传播高分辨率通道，并帮助网络达到良好的真实分辨率。

# 第十章

1.  PID 控制因为只需要解决简单的代数方程。回想一下，MPC 需要在实时中解决多元优化问题，这需要非常高的处理能力以确保足够低的延迟以进行驾驶。

1.  PID 控制中的积分项通过根据系统的累积误差应用控制输入来纠正系统中的任何稳态偏差。

1.  PID 控制中的微分项通过根据误差的时间变化率调整控制输入来纠正对设定点的超调。

1.  成本用于为轨迹分配一个值，该值在碰撞成本、连续动作成本、使用执行器的成本以及未到达目的地的成本等情况下被最小化。

    约束是系统的物理限制，例如转弯半径、最大横向和纵向加速度、车辆动力学和最大转向角度。

# 第十一章

1.  地图绘制旨在将有关环境中的可导航空间的信息存储起来，而定位旨在确定机器人在环境中的位置。

1.  `odom_frame`.

1.  SLAM 是必需的，因为地图永远不会完全拥有关于环境的最新信息。因此，你需要不断地在移动过程中创建可导航空间的地图。SLAM 还提供了一种在没有昂贵的高精度惯性测量单元（IMU）设备的情况下绘制环境的方法。

1.  `assets_writer_cart_3d.lua`.
